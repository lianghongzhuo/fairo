defaults:
  - _self_
  - robot_client: empty_statistics_client  # [bullet_sim, franka_hardware, empty_statistics_client]
  - robot_model: franka_panda

server_exec: run_server
ip: 0.0.0.0
port: 50051
timeout: 15
hz: 1000
use_real_time: false